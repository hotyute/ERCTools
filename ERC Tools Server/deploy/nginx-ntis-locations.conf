# Included inside the existing os-auto.org HTTPS server block.
location = /ntis/event {
    auth_basic "ERC Tools NTIS receiver";
    auth_basic_user_file /etc/nginx/ntis-event.htpasswd;
    limit_except GET POST { deny all; }

    client_max_body_size 512m;
    proxy_request_buffering on;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 10s;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
    proxy_pass http://127.0.0.1:18080;
}

location = /ntis/network-model {
    auth_basic "ERC Tools NTIS receiver";
    auth_basic_user_file /etc/nginx/ntis-model.htpasswd;
    limit_except GET POST { deny all; }

    client_max_body_size 512m;
    proxy_request_buffering on;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 10s;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
    proxy_pass http://127.0.0.1:18080;
}
